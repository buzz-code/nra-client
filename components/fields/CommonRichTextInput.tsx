import React from 'react';
import { RichTextInput, RichTextInputProps } from 'ra-input-rich-text';

export const CommonRichTextInput = (props: RichTextInputProps) => {
    return (
        <RichTextInput {...props} />
    );
};